<%= simple_form_for @cocktail do |f| %>
  <%= f.input :name %>
  <%= f.input :category, collection: Cocktail::CATEGORIES %>
  <%= f.input :photo, as: :attachinary, class: "list-inline" %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
